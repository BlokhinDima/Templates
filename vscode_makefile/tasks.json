{
    "tasks": [
        {
            "type": "shell",
            "label": "make",
            "command": "make",
            "args": ["application"],
            "options": {
                "cwd": "${workspaceRoot}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Задача создана отладчиком."
        }
    ],
    "version": "2.0.0"
}